spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/gaurav700/microservices-config-server
          username: gaurav700
          password: <github person access token>
          default-label: main

server:
  port: 8888
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
